# Deutsch Wörter

导入自己的德语词库、离线可用、无后端的背词 PWA。词由用户提供，数据留在设备上。

## 怎么跟维护者讲解

用中文。讲一个问题或一条建议时，按这个顺序，**不要跳步**：

1. **先讲现实里这是什么事。** 从学德语的人的角度起头，给具体的德语例句配中文
   （`Ich habe gekauft` 我买了 ／ `Ich bin gegangen` 我走了）。需要的话给一个
   "说错了像什么"的类比——`Ich habe gegangen` 约等于中文说"我把饭吃在了"。
2. **再说词库里已经有什么。** 拿真实卡片举例，不要给统计表。
3. **再说应用现在漏掉了什么。**
4. **最后才说改动。** 画一个模拟界面（题目长什么样），而不是描述代码怎么写。
5. 结尾问一句哪段没讲清楚。

**开头不要甩函数名、文件行号、百分比对照表。** 那些留到真正动手写代码的时候再用。
一次只讲一个点，例子要具体到能想象出屏幕上是什么样。
（这条是维护者明确要求的：先按上面这样讲一遍才听懂，之前的技术写法讲了两轮都没讲通。）

## 命令

```sh
npm ci
npx playwright install chromium   # Linux 加 --with-deps
npm run dev                       # 构建后在 127.0.0.1:4321 提供 dist/
npm run verify                    # tsc + vitest + build + playwright，CI 跑的就是这个
```

改 `src/`，`scripts/build.mjs` 生成 `dist/`。`dist/` 从不提交。

## 动手前必须知道的几条

这些不是风格偏好，是踩过的坑。违反其中任何一条都会静默毁掉用户数据。

**卡片 id = `sha256(level|chapter|de)` 截前 10 位。改 `de` 就等于换了一张卡，那个词的学习进度归零。**
改 `zh`/`en`/`grammar`/`example` 不会。`tests/data.test.ts` 把预期哈希钉成字面量：
**哪天这个测试需要改，就说明有人的学习历史刚被作废了。** SHA-256 是手写的，不用
`crypto.subtle`（非安全上下文里不存在，而且是异步的）。

**`src/store.js` 独占所有 localStorage 读写，写入是批量的（1500ms 窗口）。**
任何需要读回自己状态的模块必须在内存里保留一份并合并进去——在一个 flush 窗口内重新读存储
会拿到过期值，然后把待写入的改动覆盖掉。`DWStore.onMigrated()` 是给模块重新水合用的。

**`src/insight.js` 只说几乎无例外的规则。** 仅仅"恰好以这几个字母结尾"的词逐个列在例外里
（`der Kuchen` 不是指小词）。规则算出的性别和卡片冠词不一致时必须闭嘴——有测试守着。
编一个解释比空面板更糟。

**Service Worker 缓存名由内容推导**，所以一次部署不会把老访客永久钉在旧 JS 上。

**测试不碰网络。** Wikimedia 是打桩的。e2e 用 `tests/fixtures/deck.json`，走应用自己的
解析器导入——导入一坏，整套测试就红。

## 代码地图

| 文件 | 职责 |
| --- | --- |
| `src/learn.core.js` | 背词模式：今日任务、三层学习、拼写、SRS、发音 |
| `src/index.html` | 单词检测模式（作答通过 `link-addon.js` 回写背词进度） |
| `src/store.js` | localStorage 全部读写、迁移、备份（含自动写文件夹） |
| `src/deck.js` | 词库解析、卡片 id、IndexedDB、`DWPatches` 补丁层 |
| `src/insight.js` | 巧记规则（性别/复合词/前缀/元音变化类/sein/多义），当场算；**词形栏唯一的解析器** |
| `src/drills-addon.js` | 专项训练：性别、复数、动词变位、haben/sein、介词+格、例句填空、听写 |
| `src/wrongbook-addon.js` | 拼写错题本 + 错因分析 |
| `src/mastered-addon.js` | 已掌握档案 |
| `src/browse-addon.js` | 查词 + 章节地图 + 「学到哪了」的位置 |
| `src/backup-addon.js` | 备份面板（文件夹 / 分享 / 下载） |
| `src/edit-addon.js` | 应用内编辑词条（只改 zh/en/grammar/example） |
| `src/link-addon.js` | 检测 ↔ 背词进度互通 |

---

# 待办：让引擎读懂词库

**背景（2026-09 评估）。** 核心循环是扎实的。真正的缺口是词库已经比应用聪明了——
当前这份 5434 词的库里有 **1670 条例句、380 条动词变位、84 条反身动词、174 条介词支配格、
175 条"本章义项"**，应用把它们全部当成一段文本显示了事。所以优先级最高的不是加新功能，
是让引擎用上已经在那里的数据。

下面每条都在代码里核实过，带 `文件:行号` 锚点。

## P0 · 三个 bug（惩罚学得更好的人）　✅ **三条全部已修**

**1. 反身动词：写对了反而判错。**　✅ **已修**
`LspellAccepted` 现在把 `sich` 当成和名词冠词一样的可选成分，两个方向都认。
真实词库里 84 条反身词，其中 82 条 `de` 是裸动词、2 条 `de` 自带 `sich`（同一份词库两种写法都有），
现在带不带 `sich` 都判对，且没有一条非反身动词被误放行。
错题本原本拷了一份同样的规则，已改成委托给同一个函数——那份拷贝就是它继续判错的原因。
<details><summary>原始记录</summary>
`LspellAccepted`（`learn.core.js:92`）比对 `de`，而反身动词的 `de` 是裸的 `freuen`，
`sich` 只写在 `zh` 里（`（sich auf +A）期待`）。实测：输入 `sich freuen` 判**错**，
输入 `freuen` 判对。**79 条反身动词全部受影响。**
修法：拼写比对时允许去掉前置 `sich`——和名词去冠词是同一套逻辑，就在同一个函数里。
</details>

**2. 「这里：/hier:」在选择题里漏答案。**　✅ **已修**
出题时剥掉义项前缀：四个选项现在都是干净的释义，格式不再指向答案。真实词库 **175 条**受影响。
剥的是「这里：」「此处：」「hier:」，冒号是必需的——`hier`「这里」本身和 `hierarchisch` 不受影响；
剥完为空就退回原文，宁可漏一条也不要出个空选项。卡片正面（`Lreverse` 的题干、反馈里的释义）
照旧显示完整的 `这里：…`，那是学生要读的语境，只有**选项**被剥。
顺带修掉了这个 tell 一直在遮的另一个 bug：同一个德语词在词库里出现 **518 次**分属不同卡
（`A1K1 wie → 这里：什么` 和 `A1K1 wie → 这里：像；如同` 就在同一章），
`Ldistractors` 原先只按释义文字去重，于是「wie 是什么意思」会同时列出两个都对的选项，
学生选中另一个被判错。现在同形词直接排除出干扰项——**27 条同章、280 条同级别**受影响。
<details><summary>原始记录</summary>
`Ldistractors`（`learn.core.js:72`）只按 `Lnorm` 去重，不剥义项前缀。四个选项里唯一
一个以「这里：」开头、英文小字还写着 `hier:`，学生很快学会看格式选答案。**175 条受影响。**
修法：出题时剥掉前缀，或反过来给所有选项加同样前缀。
</details>

**3. 「这个我已经会」是永久出局。**　✅ **已修**
「已掌握」现在是「间隔很长」，不再是「永远不问」。到期的已掌握词每天最多 5 个回到今日任务
抽查一次（打开拼写就考拼写，关了就考「看意思认德语」），答对推到更远，答错 `Lrecord` 本来
就会清掉 `known`、退回学习中——原本那个 +30 天的 `due` 从死代码变成了真正的抽查时间。
已掌握面板和两份 README 里「永久退出」的说法都改了。
<details><summary>原始记录</summary>
`LintroDone`（`learn.core.js:87`）把 `due` 设成 +30 天，但 `Lmastered`（`:63`）一看到
`known` 就直接 `return true`，永远不再进复习——那 30 天是死代码。
对一个要学一年的 5434 词库，这个按钮点得越多漏得越多。至少让 `known` 词以低频抽检。
</details>

## P1 · 用上已有数据（投入最小、收益最大）

**4. 例句 cloze + 整句发音。**　✅ **已做**
新增「例句填空」专项：真实词库 1670 条例句里 **1413 条**能可靠定位并出题。
词在句中变形的（`international` → `internationalen`）挖掉整个变化形式，答原形或变化形式都算对；
定位不到的（可分动词拆开、`sein` → `bin`）**不出题**，而不是出个错题。
例句现在先只显示德文，中文点开才看；🔊 按钮送整句——`LaudioUrl` 只匹配单词，
所以句子会自动落到合成语音，不用改发音那条链路。
<details><summary>原始记录</summary>
1670 条例句全是 `德文。（中文）` 的严格格式，且保证词条一定出现在德文句中（有脚本验证过）。
挖掉词条让人填 = 现成的语境练习。而且 **312 条动词例句里出现的是变位形式**
（`Ich gehe…`、`er nimmt … teil`），cloze 顺手把变位练了。
同一份数据还能做两件事：先只显示德文、点开才显示中文（现在一起显示）；
把整句送 TTS——**目前 `Lspeak` 从头到尾只念过单词**（`learn.core.js:161`、
`drills-addon.js:149`），一句德语句子听力都没有。
</details>

**5. 动词专项（两个新 drill）。**　✅ **两个都已做**
新增「haben / sein」专项：真实词库 **379 条**可出题（haben 273 · sein 106）。
准确率按助动词分开报，理由和性别题一样——一个总分会美化学习者。
唯一出不了题的是 `geben`（`es gibt, es hat gegeben`，无人称，助动词前面还有个 `es`）。
顺带修了 `LdrillAccuracy`：它原本写死只统计名词，听写的准确率一直是拿名词集算的。

新增「动词变位」专项，真实词库 **368 个动词**能出题：
- **91 个变元音**（`nehmen → er nimmt`，不是 nehmt）、**157 个可分**（`aufstehen → er steht auf`，
  前缀甩到句末）、**120 个规则**。**144 个带 Präteritum** 的按位置轮到过去式
  （`LconjAsk(c, i)` 按队列位置交替，不用随机数，所以一轮的题是可复现的）
- 准确率按这四类分开报——一个总分会把唯一重要的那类（没人提醒过你的元音变化）藏起来
- 答完不管问的是哪一形，都把整行摆出来（`er spricht · er sprach · er hat gesprochen`），
  因为强变化动词的三个形本来就是一起记的
- 多词词条（`spazieren gehen`、`los sein`，共 5 条）不出题
<details><summary>原始记录</summary>
语法栏里有 **380 条**动词变位（`er geht, ist gegangen`），其中 **374 条**能直接解析出分词、
可以出题（A1/A2 两分形 228 条，B1 带 Präteritum 三分形 146 条）。
剩下 6 条是多词动词（`es gibt`、`spazieren gehen`、`ernst nehmen`…），分词是两个词，要单独处理。
复数专项已经在解析语法栏记号了，同样的思路：
- **haben / sein 助动词**：可出题的 374 条里 **272 个 hat、102 个 ist**。
  值得做是因为它**蒙不出来**：性别题闭眼全猜 `die` 就有 45 分，所以那边的正确率
  不得不按冠词分开报；助动词只有两个按钮，真会和瞎猜几轮就分得出来。
  而且 `Ich habe gegangen` 是母语者立刻听得出的错，现在却从没考过。
- **变位填写**：给不定式，写第三人称或过去分词（`nehmen → er nimmt`，不是 `nehmt`）。
</details>

**6. 支配格（Rektion）专项。**　✅ **已做**
新增「介词 + 格」专项，真实词库 **174 条全部能出题**，分成两种题型（分开计分，因为是两种能力）：
- **128 条搭配题**：给词和意思，选「介词 + 格」（`warten` → `auf + 四格`）。
  5 条动词支配两个介词（`sprechen mit +D / über +A`），两个都算对，
  而且**另一个正确答案绝不会作为干扰项出现**
- **46 条支配题**：给介词，选它支配的格。其中 **1 条是 `helfen`**——
  它不是介词，是直接支配三格的动词（`Ich helfe dir`，不是 dich），
  所以题面措辞不同（「这个词后面直接跟哪个格？」），但确实该考
- 提示里的括号会先摘掉：`等待（auf +A 等某人）` → 只显示「等待」。
  不摘等于把答案印在题面上——和第 2 条是同一类 bug
- 介词必须在白名单里才算数（补上了 `ab / außer / außerhalb / innerhalb / entgegen`）；
  裸的格标记必须**紧跟在括号开头**（词库就是这么写的：`帮助（+D 帮某人）`），
  散落在正文里的 `+A` 不出题
- 干扰项不够（小词库）时不出题，而不是出个只有一个按钮的题

**7. 巧记补动词规则。**　✅ **已做**
三条都做了，全部从词库自己的变位栏推导，不预存任何词表：

**元音变化类。** `bleiben–blieb–geblieben` 和 `schreiben–schrieb–geschrieben` 是一个规律、
不是两条事实。三个形的词干元音直接读出来（要先剥前缀——`verschwinden` 的第一个元音是 `ver` 的 e）。
真实词库 **119 个动词**能归类，`ei→ie→ie`、`e→a→o`、`a→ie→a`、`ie→o→o` 这些是大头。
**两道闸门**：三元组必须是德语真有的那十几个类之一，**并且**当前词库里至少 3 个成员。
剥前缀剥错了会产出 `u–u–u` 这种三元组，白名单直接把它挡掉——闸门的作用就是「解析出错也编不出规律来」。

**haben/sein。** 关键决定：**不预测助动词，只解释卡片已经说了的那个。**
「表位移用 sein」这条规则解释了本词库 102 条 `ist` 里的 91 条（89%），
但会在 8 条 `hat` 上说错（及物的 `anziehen` 用 haben、不及物的 `umziehen` 用 sein）——
89% 够不上「只说几乎无例外的规则」。而卡片自己写着 `ist gegangen`，
所以只在卡片说了 `ist` 时才给它归类，**结构上不可能和卡片矛盾**。
真实词库 95 条命中，**0 条矛盾**（端到端测试拿整副牌验这一条，和性别那条同一个守卫）。

**同一个词的别的意思。** 全库 207 个词头分属多张卡（538 张）。
`der Rock` 裙子 ↔ 摇滚乐；`der Gefallen` 人情 ↔ `das Gefallen` 喜爱——
后面这种冠词不同意思就不同的，标题会换成「换个性别就换个意思」。
这正是第 2 条的另一半：那边把同形词**排除出干扰项**，这边把它**显示出来**。
比较和显示都先剥掉 `这里：`。

**顺带并掉了一份重复解析。** 词形栏原来被读三遍（`LauxOf`、`LconjOf`，加上这里）。
现在只有 `DWInsight.Lforms` 一个解析器，专项训练委托给它——
专项和解释再也不可能对同一栏有两种理解。

## P2 · 记忆算法：适合一个月，不适合一年

**8. 间隔是写死的四级。**　✅ **已修**
阶梯补到 10 分钟 → 1 天 → 3 天 → 14 天 → **30 天 → 90 天**，每一级都要再写对一次才推进。
`hard` 从「只记不用」变成：标过「很难记」的词下次间隔减半，直到它通过一次拼写为止。
今日任务没词时会告诉你「明天有 N 个到期」——「今天没有」和「永远没有」读起来完全不同。
<details><summary>原始记录</summary>
`Linterval`（`learn.core.js:64`）：10 分钟 → 1 天 → 3 天 → 14 天，第三轮过了就"已掌握"、
永远退出复习（`Lmastered`，`:63`）。A1 到 B1 要学一年，"18 天内答对 3 次"离长期记住还远。
- 加 30 天 / 90 天长尾，或每天从已掌握里抽 5 个进今日任务
- `hard` 计数器只记不用（`:87`）——点"很难记"的词应该间隔更短
- 今日任务加一行"明天到期 N 个"，让人有预期
</details>

## P3 · 5434 词的规模问题

**9. 今日任务的新词不认章节。**　✅ **已修**
词库现在有一个「学到哪了」的位置（存在 `prefs.posLevel/posChapter`），今日任务的新词
从那一章开始往后走；这一章学完了自动认下一章（`LposGroup` 是推导出来的，不写回存储，
所以渲染时不会有副作用）；**前面全学完了才回头**捡之前跳过的章，而不是谎称"学完了"。
没设位置的老用户行为和以前完全一样（`LposIndex()` 返回 -1 就从头开始）。
首页第一行现在先说你在哪（`LrenderPosBar`），再说今天有多少。
章节按级别+章号排序（`LchapGroups`），不按文件行序——真实词库两者恰好一致，
但新加的章会追加在文件末尾，排序保证它落在该在的位置。
整章标为已会在章节地图里（见第 10 条），到期时间在 30–90 天之间铺开，
避免一整章的抽查在同一天砸下来。

**10. 没有查词。**　✅ **已做**
新增 `browse-addon.js`：一个面板，**空着的时候是 36 章的地图，一打字就变成搜索结果**——
这两件事本来就是同一个问题（"这词在哪"和"我在哪"）的两面。
- 德语、中文、英文、词形栏都查；结果标出所在章和当前状态（还没学 / 学习中 / 待复习 / 已掌握）
- **不打变音字母也能查**：`tur` 和 `tuer` 都能找到 `die Tür`。折的是词库那一侧不是查询那一侧，
  否则 `Bruder` 会被当成 `Brüder` 弄丢
- 排序：整词优先于包含，**两种语言都算**——输入「花」时 `die Blume`（花）排在
  `der Garten`（花园）前面。`这里：` 前缀先剥掉再比，不然带前缀的义项永远排不到前面
- 地图上每章一格，显示已掌握/学习中的进度条；点一格可以「从这一章开始」
  「这之前的 N 个我都会了」「这一章我都会了」「看这一章的词」

**11. 两套进度各算各的。**　✅ **已修**
新增 `src/link-addon.js`：检测的作答**回写 `learnProgress`**，两个方向都通。
- 两种模式不是同一种练习，但**是同一批技能**：英→德是打出德语，等于拼写层；
  德→英是看德语说意思，等于认词层。所以按对应的 `type` 走**同一套状态转移**
  （把 `Lrecord` 里的转移抽成了 `LapplyAnswer`——错题本当年就是因为拷了一份规则才走偏的）
- **绝不「引入」新词**：`!s.introduced` 就不写。否则一轮 200 题全库检测会把几百个
  从没学过的词倒进今日任务，正好和今日任务的用途相反
- 排期被别的模式悄悄改动，用户会当成 bug，所以检测的反馈里直接写出来：
  「已记入背词进度：下次复习两周后」/「回到复习队列」
- 反过来也通：勾了「优先抽错词 / 生词」时，**背词里今天到期的词排在最前面**
  （复选框的文案也改了）。不勾就还是全库平均抽

`index.html` 里的 `mastery` 保留，它只用来做检测自己的加权抽取；
已掌握的词本来就被 `mastered-addon` 挡在检测之外，所以不会在那边被打回。

## P4 · 本地优先的代价

**12. 备份提醒太弱。**　✅ **已修**
提醒改成按**「上次备份以来新增了多少学习量」**触发（`workUnits()`，只增不减：
每个学过的词算 1 + 完成轮数 + 已掌握加 1），时间和学习量**任一**达标就提醒。
原来的规则是 14 天 + ≥50 词，一周学 400 个词的人一句话都收不到。
提醒的措辞也换了：不说「14 天」，说「上次备份之后你又学了 213 个词」。

备份的去处按设备能力给，而不是只有一个下载按钮（新增 `src/backup-addon.js` 的面板）：
- **电脑版 Chrome / Edge**：`showDirectoryPicker` 选一个文件夹，**每次打开应用自动写一份**。
  选 iCloud / Google 云端硬盘的文件夹就等于同步到云上，这里不需要服务器也不需要账号。
  句柄存在 IndexedDB（`DWDeck.getMeta/putMeta`，localStorage 存不了句柄）
- **手机**：`navigator.share` 打开系统分享菜单——这是文件在手机上进 iCloud 的实际路径
- **其他**：照旧下载

两个细节：
- **永远写两个文件**：覆盖唯一一份副本，正是备份可能毁掉它要保护的东西的时刻。
  先把旧的复制成 `-previous.json` 再写新的
- Chromium 重启后会掉权限，重新授权需要用户手势，所以那条通知本身就是那个手势

**13. 词条本身不能改。**　✅ **已做**
新增 `src/edit-addon.js` + `DWPatches`（在 `deck.js` 里）：改动作为**按 id 的补丁层**
存进 IndexedDB，**重新导入词库也不丢**。入口有两个：查词结果的每一行，和学习卡片的详情区
（`Ldetails`，只在介绍页和答案揭晓后出现，不会在题目还开着的时候露出来）。
- **只能改 `zh` / `en` / `grammar` / `example`。`de` 是只读的**，面板上写明了为什么：
  卡片编号是 `level|chapter|de` 的哈希，改掉它不是编辑一张卡，是丢掉一张卡和它的全部历史
- 每个字段都带一句说明这栏是给谁用的（`Plural: die Häuser`、`er nimmt, hat genommen`、
  `Deutscher Satz.（中文）`），因为专项训练完全靠这几栏的格式
- 「恢复词库原文」要能恢复，所以第一次编辑前先把原值记下来（`DWPatches.remember`）
- 改完要让**已经渲染出来的界面**跟着变：`LcardsChanged()` 清掉各模块的缓存
  （`ZH`、`LbrowseHays`、`LrektionCache`），并重画开着的查词面板和当前题目。
  导入备份恢复补丁走的是同一个函数——这是测试里发现的：原本导入时开着的查词面板不会刷新
- 补丁进备份（`snapshot().cardPatches`，version 6），导入时合并；
  合并冲突时**保留本机的**，因为你正看着的就是本机这份

**14. 杂项。**　⏸ **不做**（维护者 2026-09 明确说深色模式不需要）
没有深色模式（`learn.css` 和 `index.html` 里 0 处 `prefers-color-scheme`，
几个 overlay 还硬编码了 `#fff`，现在新增的六个面板也是）。
形容词比较级只有 9 条，不值得做专项。

---

**已完成：1–13 全部。** 只剩第 14 条的深色模式（维护者明确说不需要）。

这一轮下来，词库里原本只被当成文本显示的每一栏都进了学习流程：
例句 1413 条能出填空题、变位 368 个动词能出题、支配格 174 条、`ist` 95 条能解释、
119 个动词能归到元音变化类、538 张卡能指出同一个词的别的意思。
「越认真学越吃亏」的三个判错没了；「学完」不再等于「从此不问」；
5434 词不再是一堆无法定位的行（有位置、有地图、有查词）；
两种模式共用一套进度；一年的记录有了真正的备份路径；词条可以在应用里改。

**下一个建议：** 没有待办了。真要挑的话——
深色模式（第 14 条，维护者说不需要）；
或者拿真实使用检验一下新加的六个面板在手机上会不会太挤。

**跑测试要注意：** 这台机器上 Playwright 的浏览器版本和项目 pin 的对不上，
用仓库早就预留好的变量绕开，别去跑 `playwright install`：
`PLAYWRIGHT_CHROMIUM_PATH=/opt/pw-browsers/chromium-1194/chrome-linux/chrome npm run verify`
