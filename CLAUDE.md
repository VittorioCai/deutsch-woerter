# Deutsch Wörter

导入自己的德语词库、离线可用、无后端的背词 PWA。词由用户提供，数据留在设备上。

## 怎么跟维护者讲解

用中文。讲一个问题或一条建议时，按这个顺序，**不要跳步**：

1. **先讲现实里这是什么事。** 从学德语的人的角度起头，给具体的德语例句配中文
   （`Ich habe gekauft` 我买了 ／ `Ich bin gegangen` 我走了）。需要的话给一个
   "说错了像什么"的类比——`Ich habe gegangen` 约等于中文说"我把饭吃在了"。
2. **再说词库里已经有什么。** 拿真实卡片举例，不要给统计表。
3. **再说应用现在漏掉了什么。**
4. **最后才说改动。** 画一个模拟界面（题目长什么样），而不是描述代码怎么写。
5. 结尾问一句哪段没讲清楚。

**开头不要甩函数名、文件行号、百分比对照表。** 那些留到真正动手写代码的时候再用。
一次只讲一个点，例子要具体到能想象出屏幕上是什么样。
（这条是维护者明确要求的：先按上面这样讲一遍才听懂，之前的技术写法讲了两轮都没讲通。）

## 命令

```sh
npm ci
npx playwright install chromium   # Linux 加 --with-deps
npm run dev                       # 构建后在 127.0.0.1:4321 提供 dist/
npm run verify                    # tsc + vitest + build + playwright，CI 跑的就是这个
```

改 `src/`，`scripts/build.mjs` 生成 `dist/`。`dist/` 从不提交。

## 动手前必须知道的几条

这些不是风格偏好，是踩过的坑。违反其中任何一条都会静默毁掉用户数据。

**卡片 id = `sha256(level|chapter|de)` 截前 10 位。改 `de` 就等于换了一张卡，那个词的学习进度归零。**
改 `zh`/`en`/`grammar`/`example` 不会。`tests/data.test.ts` 把预期哈希钉成字面量：
**哪天这个测试需要改，就说明有人的学习历史刚被作废了。** SHA-256 是手写的，不用
`crypto.subtle`（非安全上下文里不存在，而且是异步的）。

**`src/store.js` 独占所有 localStorage 读写，写入是批量的（1500ms 窗口）。**
任何需要读回自己状态的模块必须在内存里保留一份并合并进去——在一个 flush 窗口内重新读存储
会拿到过期值，然后把待写入的改动覆盖掉。`DWStore.onMigrated()` 是给模块重新水合用的。

**`src/insight.js` 只说几乎无例外的规则。** 仅仅"恰好以这几个字母结尾"的词逐个列在例外里
（`der Kuchen` 不是指小词）。规则算出的性别和卡片冠词不一致时必须闭嘴——有测试守着。
编一个解释比空面板更糟。

**Service Worker 缓存名由内容推导**，所以一次部署不会把老访客永久钉在旧 JS 上。

**测试不碰网络。** Wikimedia 是打桩的。e2e 用 `tests/fixtures/deck.json`，走应用自己的
解析器导入——导入一坏，整套测试就红。

## 代码地图

| 文件 | 职责 |
| --- | --- |
| `src/learn.core.js` | 背词模式：今日任务、三层学习、拼写、SRS、发音 |
| `src/index.html` | 单词检测模式（独立的一套进度模型） |
| `src/store.js` | localStorage 全部读写、迁移、备份提醒 |
| `src/deck.js` | 词库解析、卡片 id、IndexedDB |
| `src/insight.js` | 巧记规则表（性别/复合词/前缀，当场算） |
| `src/drills-addon.js` | 专项训练：性别、复数、听写 |
| `src/wrongbook-addon.js` | 拼写错题本 + 错因分析 |
| `src/mastered-addon.js` | 已掌握档案 |

---

# 待办：让引擎读懂词库

**背景（2026-09 评估）。** 核心循环是扎实的。真正的缺口是词库已经比应用聪明了——
当前这份 5434 词的库里有 **1670 条例句、380 条动词变位、79 条反身标记、174 条介词支配格、
175 条"本章义项"**，应用把它们全部当成一段文本显示了事。所以优先级最高的不是加新功能，
是让引擎用上已经在那里的数据。

下面每条都在代码里核实过，带 `文件:行号` 锚点。

## P0 · 三个 bug（惩罚学得更好的人）

**1. 反身动词：写对了反而判错。**
`LspellAccepted`（`learn.core.js:92`）比对 `de`，而反身动词的 `de` 是裸的 `freuen`，
`sich` 只写在 `zh` 里（`（sich auf +A）期待`）。实测：输入 `sich freuen` 判**错**，
输入 `freuen` 判对。**79 条反身动词全部受影响。**
修法：拼写比对时允许去掉前置 `sich`——和名词去冠词是同一套逻辑，就在同一个函数里。

**2. 「这里：/hier:」在选择题里漏答案。**
`Ldistractors`（`learn.core.js:72`）只按 `Lnorm` 去重，不剥义项前缀。四个选项里唯一
一个以「这里：」开头、英文小字还写着 `hier:`，学生很快学会看格式选答案。**175 条受影响。**
修法：出题时剥掉前缀，或反过来给所有选项加同样前缀。

**3. 「这个我已经会」是永久出局。**
`LintroDone`（`learn.core.js:87`）把 `due` 设成 +30 天，但 `Lmastered`（`:63`）一看到
`known` 就直接 `return true`，永远不再进复习——那 30 天是死代码。
对一个要学一年的 5434 词库，这个按钮点得越多漏得越多。至少让 `known` 词以低频抽检。

## P1 · 用上已有数据（投入最小、收益最大）

**4. 例句 cloze + 整句发音。**
1670 条例句全是 `德文。（中文）` 的严格格式，且保证词条一定出现在德文句中（有脚本验证过）。
挖掉词条让人填 = 现成的语境练习。而且 **312 条动词例句里出现的是变位形式**
（`Ich gehe…`、`er nimmt … teil`），cloze 顺手把变位练了。
同一份数据还能做两件事：先只显示德文、点开才显示中文（现在一起显示）；
把整句送 TTS——**目前 `Lspeak` 从头到尾只念过单词**（`learn.core.js:161`、
`drills-addon.js:149`），一句德语句子听力都没有。

**5. 动词专项（两个新 drill）。**
语法栏里有 **380 条**动词变位（`er geht, ist gegangen`），其中 **374 条**能直接解析出分词、
可以出题（A1/A2 两分形 228 条，B1 带 Präteritum 三分形 146 条）。
剩下 6 条是多词动词（`es gibt`、`spazieren gehen`、`ernst nehmen`…），分词是两个词，要单独处理。
复数专项已经在解析语法栏记号了，同样的思路：
- **haben / sein 助动词**：可出题的 374 条里 **272 个 hat、102 个 ist**。
  值得做是因为它**蒙不出来**：性别题闭眼全猜 `die` 就有 45 分，所以那边的正确率
  不得不按冠词分开报；助动词只有两个按钮，真会和瞎猜几轮就分得出来。
  而且 `Ich habe gegangen` 是母语者立刻听得出的错，现在却从没考过。
- **变位填写**：给不定式，写第三人称或过去分词（`nehmen → er nimmt`，不是 `nehmt`）。

**6. 支配格（Rektion）专项。**
174 条卡片的 `zh` 里带 `（auf +A）`、`（mit +D）`。这正是 B1 考试考、而「看意思选德语」
永远测不到的东西。题型：给动词和意思，选介词 + 格。数据已经在那里，只差一个正则。

**7. 巧记补动词规则。**
`insight.js` 已经会讲可分/不可分前缀，但从变位栏还能算出更多：强变化的元音变化类
（`bleiben–blieb–geblieben` 和 `schreiben–schrieb–geschrieben` 是一类）；
同一个词在不同章的义项互链（`der Gefallen` 人情 ↔ `das Gefallen` 喜爱、
`der Rock` 裙子 ↔ 摇滚）。

**「ist = 位移/状态变化」这条规则已经拿本词库验过，不能直接照搬：**
它解释了 102 条 `ist` 里的 91 条（89%），但会把 8 条 `hat` 误判成 `ist` ——
`ziehen` 一家分裂（及物的"拉、穿" `anziehen` 用 haben，不及物的"搬家" `umziehen` 用 sein）。
89% 达不到"只说几乎无例外的规则"的门槛。
**但这不挡路**：照搬 GENDER 表已有的「规则 + 手写例外清单」形状即可
（`{ suf, art, not: [...] }`），ziehen 一家进 `not`。
`ist` 里规则天生解释不了的那批（`sein`、`bleiben`、`passieren`、`gelingen`、`geraten`…）
本来就该死记，巧记要闭嘴。

## P2 · 记忆算法：适合一个月，不适合一年

**8. 间隔是写死的四级。**
`Linterval`（`learn.core.js:64`）：10 分钟 → 1 天 → 3 天 → 14 天，第三轮过了就"已掌握"、
永远退出复习（`Lmastered`，`:63`）。A1 到 B1 要学一年，"18 天内答对 3 次"离长期记住还远。
- 加 30 天 / 90 天长尾，或每天从已掌握里抽 5 个进今日任务
- `hard` 计数器只记不用（`:87`）——点"很难记"的词应该间隔更短
- 今日任务加一行"明天到期 N 个"，让人有预期

## P3 · 5434 词的规模问题

**9. 今日任务的新词不认章节。**
`LfreshCards`（`learn.core.js:56`）按词库顺序取前 N 个没学过的——学到 A2K7 的人点
"今日任务"永远先喂 A1K1 的剩饭。需要一个"当前学到哪一章"的位置，配"整章/整级标为已会"
（现在只能一张一张点）。首次导入时就该问一句"你学到哪了"。

**10. 没有查词。** 全库 0 个搜索框。5434 词、36 章，课上遇到一个词想查在哪章、什么意思，
做不到。最便宜的功能之一。顺带一张 36 章的进度地图，比首页四个数字更贴合"跟着课本走"的用法。

**11. 两套进度各算各的。**
单词检测有自己的 `mastery` 模型（`index.html:114-115`），加权随机、没有排期，
和背词模式的 `learnProgress` 互不相通——同一个词可能一边"已掌握"、另一边"没学过"。
要么让检测结果回写 `learnProgress`，要么把它明确定位成"考前模拟"，别再叫"检测"。

## P4 · 本地优先的代价

**12. 备份提醒太弱。** `store.js` 的 `backupReminder()`：14 天 + ≥50 词才提醒。
一年的学习记录只存在一个浏览器的 localStorage 里，风险和产品承诺不匹配。不加服务器也能做：
按"上次备份以来新增了多少进度"触发；或用 File System Access API 让用户选一个文件夹自动写备份
（选 iCloud/Drive 文件夹就等于同步了）。

**13. 词条本身不能改。** 校对、补释义、补例句目前只能在应用外做。
一个"编辑此词条"、把改动作为**按 id 的补丁层**存进 IndexedDB（重新导入词库也不丢），
就能让用户自己维护自己的词库，完全符合"词由你提供"的定位。
注意：补丁只能改 `zh`/`en`/`grammar`/`example`——改 `de` 会重置进度，见上面的不变量。

**14. 杂项。** 没有深色模式（`learn.css` 和 `index.html` 里 0 处 `prefers-color-scheme`，
几个 overlay 还硬编码了 `#fff`）。形容词比较级只有 9 条，不值得做专项。

---

**如果只做三件事：** 1（反身判错）→ 4（例句 cloze + 整句发音）→ 5（haben/sein）。
大约两三天，但会让这一周补进词库的东西第一次真正被用上。
